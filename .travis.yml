language: python

git:
  depth: false

branches:
  only:
    - master
    - dev

install:
  - pip install --upgrade numpy

script:
  - cd tests
  - ./runTests.py -d # default config
  - ./runTests.py -f # run tests, returning error code
